# Frontend Setup & Build Guide — Zesty Application

Instructions for running and building the Zesty frontend SPA locally.

---

## 🛠️ Commands

```bash
cd frontend
npm install

# Start Vite Dev Server (Port 5173 with proxy to backend 3000)
npm run dev

# Production Build
npm run build
```
Outputs compiled bundle to `frontend/dist/`.
